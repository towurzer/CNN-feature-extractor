def get_dataloader(config):
	"""
	Creates and returns the DataLoader for ...
	"""
	print(f"Image Size {config.image_size}")
	print(f"Is greyscale: {config.nc == 1}")

	dataloader = None
	return dataloader
